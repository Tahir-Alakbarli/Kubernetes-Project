apiVersion: v1
kind: Service
metadata:
  name: system-health-api-service
  namespace: kubernetes-recovery
  labels:
    app: system-health-api
spec:
  type: ClusterIP
  selector:
    app: system-health-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
