# Kubernetes-Microservice
Build a simple Python URL-shortening API, containerize it with Docker, and store the image in Amazon ECR. Deploy it to Amazon EKS using Kubernetes, with multiple replicas, services, scaling, and self-healing. Use Linux/CLI for Kubernetes and deployment operations, demonstrating AWS + Docker + Kubernetes + Python together
