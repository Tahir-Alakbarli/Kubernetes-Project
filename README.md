# Kubernetes-Microservice
Building a simple Python URL-shortening API, containerize it with Docker, and store the image in Amazon ECR. Deploying it to Amazon EKS using Kubernetes, with multiple replicas, services, scaling, and self-healing. Using Linux/CLI for Kubernetes and deployment operations, demonstrating AWS + Docker + Kubernetes + Python together
